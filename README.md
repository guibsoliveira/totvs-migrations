# totvs-migrations

Organização de scripts, views e documentação para apoiar processos de migração de dados no PostgreSQL.

## Estrutura
- `sql/` scripts SQL (schemas, migrations, views, queries, tests)
- `scripts/` automações (ex: Python)
- `docs/` documentação do processo
- `exports/` saídas locais (não versionadas)
